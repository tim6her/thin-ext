{
    "format": "Project name followed by version number as a string (no v), latest news string as project_name_news",
    "thin_ext": "1.3",
    "thin_ext_news": "Made config page available offline (using Clay), added option to temporary show second hand on tap if hidden (thanks @Martijn02!)"
}